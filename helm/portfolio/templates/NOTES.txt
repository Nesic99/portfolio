🚀 Portfolio deployed successfully!

release:   {{ .Release.Name }}
namespace: {{ .Release.Namespace }}

─────────────────────────────────────────
  ACCESS YOUR PORTFOLIO
─────────────────────────────────────────
{{- if .Values.ingress.enabled }}
  URL: http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}

  Make sure {{ .Values.ingress.host }} points to your ingress controller's
  external IP. You can find it with:

    kubectl get ingress -n {{ .Release.Namespace }}

{{- else }}
  Ingress is disabled. Port-forward to access locally:

    kubectl port-forward svc/{{ include "portfolio.fullname" . }}-frontend 8080:80 -n {{ .Release.Namespace }}

  Then visit: http://localhost:8080
{{- end }}

─────────────────────────────────────────
  USEFUL COMMANDS
─────────────────────────────────────────

  # Check pod status
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  # View backend logs
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "portfolio.name" . }}-backend

  # View frontend logs
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "portfolio.name" . }}-frontend

  # Upgrade release
  helm upgrade {{ .Release.Name }} ./helm/portfolio -n {{ .Release.Namespace }}

  # Uninstall
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

─────────────────────────────────────────
  NEXT STEPS
─────────────────────────────────────────
  1. Push your images to a registry and update values.yaml:
       backend.image.repository
       frontend.image.repository

  2. To enable TLS, set:
       ingress.tls.enabled=true
       ingress.tls.secretName=your-cert-secret

  3. To enable autoscaling:
       autoscaling.enabled=true
